# Frontend calls its own /api/* — Next.js rewrites proxy them to the backend
NEXT_PUBLIC_API_URL=/api

# Where the Express backend is actually hosted (set this in Vercel Dashboard too)
BACKEND_URL=https://porta-v2.onrender.com
